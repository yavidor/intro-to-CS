#include <stdbool.h>
#include <stdio.h>

int main(void) {
    int input = 0;
    int result = scanf("%d", &input);
    if (result == 1) {
        "YOU FCKED UP"
    }
    bool isPositive = 0;
    if (input > 0) {
        isPositive = true;
    } else {
        isPositive = false;
    }
    while (input != 0) {
    }
}
